import * as tsql from "../../../../../dist";

export const query = tsql.QueryUtil.newInstance()
    .unionLimit(1n)
    .unionLimit(2n)
    .unionLimit(3n)
    .unionLimit(4n)
    .unionLimit(5n)
    .unionLimit(6n)
    .unionLimit(7n)
    .unionLimit(8n)
    .unionLimit(9n)
    .unionLimit(10n)
    .unionLimit(11n)
    .unionLimit(12n)
    .unionLimit(13n)
    .unionLimit(14n)
    .unionLimit(15n)
    .unionLimit(16n)
    .unionLimit(17n)
    .unionLimit(18n)
    .unionLimit(19n)
    .unionLimit(20n)
    .unionLimit(21n)
    .unionLimit(22n)
    .unionLimit(23n)
    .unionLimit(24n)
    .unionLimit(25n)
    .unionLimit(26n)
    .unionLimit(27n)
    .unionLimit(28n)
    .unionLimit(29n)
    .unionLimit(30n)
    .unionLimit(31n)
    .unionLimit(32n)
    .unionLimit(33n)
    .unionLimit(34n)
    .unionLimit(35n)
    .unionLimit(36n)
    .unionLimit(37n)
    .unionLimit(38n)
    .unionLimit(39n)
    .unionLimit(40n)
    .unionLimit(41n)
    .unionLimit(42n)
    .unionLimit(43n)
    .unionLimit(44n);

query.unionLimit(45n);
